# Produção: URL pública da API, sem barra no final (build embute no bundle).
# VITE_API_URL=https://sua-api.onrender.com
#
# Dev: deixe vazio — o Vite faz proxy de /api e /socket.io para localhost:4000.
# VITE_API_URL=
